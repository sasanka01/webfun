function num() {

  var arr = numbersOnly([1, "apple", -3, "orange", 0.5]);
  var arr1 = [];
  for (var i = 0; i < array.length; i++) {
    if (typeof arr[i] === "number" ) {
        arr1 == push.arr;

    }
  }
console.log(arr1);
}
